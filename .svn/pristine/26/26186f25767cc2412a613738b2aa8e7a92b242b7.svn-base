package com.eazytec.external.controller;


import java.io.File;
import java.io.FileInputStream;
import java.io.IOException;
import java.io.InputStream;
import java.io.UnsupportedEncodingException;

import sun.misc.BASE64Decoder;
import sun.misc.BASE64Encoder;

public class FileTest {
	/*static BASE64Encoder encoder = new sun.misc.BASE64Encoder();      
    static BASE64Decoder decoder = new sun.misc.BASE64Decoder();  */
	public static void main(String[] args) throws IOException {
		/* // 将图片文件转化为字节数组字符串
	    String imgFile = "d:\\ss.jpg";// 待处理的图片
	    InputStream in = null;
	    byte[] data = null;
	    // 读取图片字节数组
	    try {
	        in = new FileInputStream(imgFile);
	        data = new byte[in.available()];
	        in.read(data);
	       
	        in.close();
	    } catch (IOException e) {
	        e.printStackTrace();
	    }
	    
	    // 返回Base64编码过的字节数组字符串
        
	    System.out.println(encoder.encode(data));*/
		/*String str = "[B@4ec4d412";
		//byte[] srtbyte = new byte[]{"B@7f39ebdb"};
		String a = "hello";
		byte[] srtbyte = null;
		try {
			 String byteName = str;
			    byte[] midbytes=byteName.getBytes("UTF8");
			    String res = new String(midbytes,"UTF-8");
			  
			        srtbyte = a.getBytes("UTF-8");
			        String c = new String(srtbyte,"UTF-8");
			        System.out.println(srtbyte);
			        System.out.println(c);
			   
		    System.out.println(res);
		   // System.out.println(res);
		} catch (UnsupportedEncodingException e) {
		    // TODO Auto-generated catch block
		    e.printStackTrace();
		}*/
	   /* File oldFile = new File("F:\新建文件夹 (3)\DremoonPlateform_20160729\DremoonPlateform_20160729\1.jpg");
		  if(!oldFile.exists())
		  {
		   oldFile.createNewFile();
		  }
		  System.out.println("修改前文件名称是："+oldFile.getName());
		  String rootPath = oldFile.getParent();
		  System.out.println("根路径是："+rootPath);
		  File newFile = new File(rootPath + File.separator + "1.jpg");
		  System.out.println("修改后文件名称是："+newFile.getName());
		  if (oldFile.renameTo(newFile)) 
		  {
		   System.out.println("修改成功!");
		  } 
		  else 
		  {
		   System.out.println("修改失败");
		  }*/
	}

}
